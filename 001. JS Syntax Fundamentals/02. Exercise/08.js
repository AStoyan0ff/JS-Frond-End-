function reverse(a, b, c) {

    const arr = [a, b, c];
    arr.reverse();
    console.log(arr.join(" "));
    
    // const res = `${c} ${b} ${a}`;
    // console.log(res);
}

reverse("A", "B", "C");
reverse("L", "A", "N");